name: Deploy WordPress Theme

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: Deploy Website
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: SamKirkland/web-deploy@1
        with:
          target-server: 43.229.84.197
          username: testftp
          password: h!8jr678K
          destination-path: /home/testftp/epic-benz.43-229-84-197.plesk.page/httpdocs/wp-content/themes/pipeline_test/
          local-dir: ./
